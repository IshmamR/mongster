{
  "name": "mongster",
  "version": "0.3.3",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "author": "IshmamR <ishmam785@gmail.com> (https://ishmam.dev)",
  "scripts": {
    "build:js": "bun run ./scripts/build.ts",
    "build:ts": "tsc -p tsconfig.build.json && bun run ./scripts/checkInlineImports.ts",
    "build": "bun run build:js && bun run build:ts",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun run tsc -p tsconfig.test.json && bun test",
    "lint": "bunx @biomejs/biome check --error-on-warnings .",
    "release": "bun run ./scripts/release.ts",
    "prepublishOnly": "bun run clean && bun run test && bun run build"
  },
  "dependencies": {},
  "peerDependencies": {
    "mongodb": "^7.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.12",
    "@inquirer/prompts": "^7.2.0",
    "@types/bun": "1.3.14",
    "mongodb-memory-server": "^11.0.1",
    "typescript": "^5.9.3"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "description": "Type-safe MongoDB ODM for TypeScript with schema validation and typed queries",
  "keywords": [
    "mongodb",
    "mongo",
    "odm",
    "typesafe",
    "typescript",
    "schema"
  ],
  "engines": {
    "bun": ">=1.3.14",
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/IshmamR/mongster.git"
  },
  "bugs": {
    "url": "https://github.com/IshmamR/mongster/issues"
  },
  "homepage": "https://mongster.ishmam.dev",
  "license": "MIT"
}
